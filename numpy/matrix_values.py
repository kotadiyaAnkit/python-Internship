import numpy as np

# Create a 3x3 matrix with values from 1 to 9
matrix = np.arange(1, 17).reshape(4, 4)  #it changes the shape of the data without changing the data itself.

# Print the matrix
print("3x3 Matrix:")
print(matrix)
